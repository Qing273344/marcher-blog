spring:
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://${server.host}:3306/marcher-blog?allowMultiQueries=true&useUnicode=true&characterEncoding=UTF-8
    username: root
    password: AES@8TLqCgSLP/ldKx2xAGpTnw==
    druid:
      # 初始化时建立物理连接的个数
      initial-size: 10
      # 最大连接池数量
      max-active: 30
      # 最小连接池数量
      min-idle: 10
      # 获取连接时最大等待时间，单位毫秒
      max-wait: 60000
      # 是否缓存preparedStatement，也就是PSCache。PSCache对支持游标的数据库性能提升巨大，比如说oracle。在mysql下建议关闭。
      pool-prepared-statements: false
      # 要启用PSCache，必须配置大于0，当大于0时，poolPreparedStatements自动触发修改为true。
      max-pool-prepared-statement-per-connection-size: 20
      # 配置间隔多久才进行一次检测，检测需要关闭的空闲连接，单位是毫秒
      time-between-eviction-runs-millis: 60000
      # 连接保持空闲而不被驱逐的最小时间
      min-evictable-idle-time-millis: 300000
      # 用来检测连接是否有效的sql，要求是一个查询语句
      validation-query: SELECT 1 FROM DUAL
      # 建议配置为true，不影响性能，并且保证安全性。申请连接的时候检测，如果空闲时间大于timeBetweenEvictionRunsMillis，执行validationQuery检测连接是否有效。
      test-while-idle: true
      # 申请连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能。
      test-on-borrow: false
      # 归还连接时执行validationQuery检测连接是否有效，做了这个配置会降低性能。
      test-on-return: false
      # druid连接池监控
      stat-view-servlet:
        enabled: true
        url-pattern: /druid/*
        login-username: admin
        login-password: admin
      filter:
        stat:
          log-slow-sql: true
          slow-sql-millis: 1000
          merge-sql: true
        wall:
          config:
            multi-statement-allow: true
  # Redis
  redis:
    database: 0
    host: ${server.host}
    port: 6379
    lettuce:
      shutdown-timeout: 5000ms
      pool:
        max-active: 8
        max-idle: 8
        max-wait: -1ms
        min-idle: 0
  cache:
    type: redis
  # 设置spring boot上传文件大小限制
  servlet:
    multipart:
      max-file-size: 30MB
      max-request-size: 30MB
  # mq
  rabbitmq:
    host: ${server.host}
    port: 5672
    username: guest
    password: guest
    virtual-host: /
    connection-timeout: 10000ms

cache:
  name: marcher
  expire-time: 180
  default-expire-time: 60

rabbitmq:
  config:
    exchange: marcher_blog_exchange
    queue: blog_content_queue
    routekey: blog_content_routekey


jwt:
  expire: 720   # token有效时长, 单位小时
  token: token   # token 存在 cookie 中
  secret: AES@u9bX3ohn2rEMj4pBh/SjSlVMbQ0piluFtz/LgHDtAG2O4BFU/QmgH1h1h2MVqAMI

oss:
  aliyunEndPoint: oss-cn-shenzhen.aliyuncs.com
  aliyunAccessKeyId: AES@IC1iNUMY0bxptfY/S+ckjI7gEVT9CaAfWHWHYxWoAwg=
  aliyunAccessKeySecret: AES@CPDzD4mKIroozQNzwGFNZ3QhlPo/VZXIfb5zIxp7pEQ=
  aliyunBucketName: marcher-test
  aliyunTempBucketName: marcher-temp

#logging:
#  config: classpath:logback-dev.xml